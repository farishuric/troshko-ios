import Foundation

enum DemoDataConfiguration {
    /// Flip this to `false` for normal app usage without automatic sample data.
    static let isEnabled = true

    /// Rolling history ending in the current month.
    static let monthCount = 18
}

